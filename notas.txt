List<TS_entry> classes;



List<TS_entry> metodos;
List<TS_entry> atributos;

tabsimolos.getClasse(classeAtual.getId()).pesquisaMetodo(idMetodo)





pesquisaClasse(String idClasse) {
    cata na tab simbolos só classes só pelo id
}

a = 5;

classeAtual.pesquisaAtributo($1);

Ts_entry metodo = new new TS_entry($2, $1, ClasseID.Metodo);

metodoAtual = metodo;
classeAtual.addMetodo(metodo);

metodoAtual.pesquisaVarLocal($1);
metodoAtual.pesquisaParam($1);


int a;

class A 
    B calculadora;
{
    void main() 
        boolean c, d, e;
    {
        calculadora.calcula(c, d);
    }
}

class B {
    public int calcula(int a, int b) {
        // blkablabla
    }
}

circulo.calculaRaio();

nodo = TS_entry do chamador do método -> circulo

nodo.getTipo() -> TS_entry da classe Circulo 

nodo.getTipo().getTipo() == Tp_OBJECT


